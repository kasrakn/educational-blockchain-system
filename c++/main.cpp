#include <iostream>
#include <math.h>
#include <time.h>
#include <thread>
#include <random>

using namespace std;

int main(){
    cout << "Number of initial nodes: ";
    int nodesNo;
    cin >> nodesNo;

    // initial nodes
    for (int i = 0; i < nodesNo; i++){
        node = Node(i);
        nodes
    }


    return 0;
}

